# BRIDGE Development Pipeline

Slash command entry point for `/bridge`. Delegates to the skill.

Read and execute `skills/bridge/orchestrator/core.md`. It contains the full pipeline definition, phase files, and module references. Read each on demand as you enter each phase.
